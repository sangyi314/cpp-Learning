#include <iostream>
#include <vector>

int main()
{
    std::vector score{84,92,76,81,56};
    std::size_t length {score.size()};
    int average {0};e score vector.
    {
    for (std::size_t index{0}; index < length ;index++)  //The type std::size_t is an unsigned integral type.
    average /= length;
    std::cout << "The average socre is:" << average << '\n' ;
    return 0;
}
